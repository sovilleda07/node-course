{
  "name": "05-noc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "tsx watch src/app.ts",
    "dev:tsnd": "tsnd --respawn --clear src/app.ts",
    "build": "rimraf ./dist && tsc",
    "start": "npm run build && node dist/app.js",
    "docker:test": "docker compose -f docker-compose.test.yml --env-file .env.test up -d",
    "test": "npm run docker:test && NODE_OPTIONS=--experimental-vm-modules jest",
    "test:watch": "npm run docker:test && NODE_OPTIONS=--experimental-vm-modules jest --watch",
    "test:coverage": "npm run docker:test && NODE_OPTIONS=--experimental-vm-modules jest --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^25.5.2",
    "@types/nodemailer": "^8.0.0",
    "jest": "^30.4.2",
    "prisma": "^7.8.0",
    "rimraf": "^6.1.3",
    "supertest": "^7.2.2",
    "ts-jest": "^29.4.11",
    "ts-node-dev": "^2.0.0",
    "tsx": "^4.22.4",
    "typescript": "^6.0.2"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "cron": "^4.4.0",
    "dotenv": "^17.4.1",
    "env-var": "^7.5.0",
    "mongoose": "^9.7.2",
    "nodemailer": "^8.0.4",
    "pg": "^8.22.0"
  }
}
